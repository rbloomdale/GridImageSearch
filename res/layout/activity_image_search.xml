<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ImageSearchActivity" >

    <EditText
        android:id="@+id/etSearchField"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:ems="10"
        android:hint="Search Words"
        android:inputType="textAutoComplete" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/btnSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/etSearchField"
        android:layout_alignBottom="@+id/etSearchField"
        android:layout_toRightOf="@+id/etSearchField"
        android:text="Search" 
        android:onClick="onImageSearch"/>

    <GridView
        android:id="@+id/gvSearchResults"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/etSearchField"
        android:layout_below="@+id/etSearchField"
        android:numColumns="3" >
    </GridView>

</RelativeLayout>
